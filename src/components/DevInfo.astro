---
const siteUrl = new URL(Astro.url.pathname, Astro.site);
---

{
  import.meta.env.DEV && (
    <div
      style={{
        position: "absolute",
        top: "10px",
        left: "10px",
        padding: "1rem 0.5rem",
        color: "black",
        background: "white",
      }}
    >
      <a href={siteUrl} target="_blank">
        {siteUrl}
      </a>
    </div>
  )
}
